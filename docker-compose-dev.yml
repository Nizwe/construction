version: '2'

services:
    nginx: 
        restart: unless-stopped
        build:
            context: ./nginx
            additional_contexts:
              frontend: ./frontend
            dockerfile: ./nginx/Dockerfile.dev
        ports:
            - 80:80

            